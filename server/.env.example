# Copy to .env and adjust for your WampServer MySQL instance
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=pms_db

# API server
PORT=3000
CORS_ORIGIN=*

# Require Bearer token on /api/* routes (set false for local debugging only)
AUTH_REQUIRED=true
SESSION_TTL_HOURS=8
